class User:

    def __int__(self, name, email, address, password):
                self.name = name
                self.email = email
                self.address = address
                self.password = password

kurt = User('k.angle', 'kangel@gmail.com', '123 Drury Lane', 'password123')
print(kurt)